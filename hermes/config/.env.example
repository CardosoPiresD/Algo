# Copier vers .env — ne JAMAIS committer le .env réel (déjà dans .gitignore).
IBKR_HOST=127.0.0.1
IBKR_PORT=7497
IBKR_CLIENT_ID=42

# Couche IA (facultative) — OpenRouter, ex. DeepSeek. Sans cette clé, le bot
# tourne en quant pur (fail-open). Récupère la clé sur openrouter.ai/keys.
OPENROUTER_API_KEY=
